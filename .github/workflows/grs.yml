name: Update README cards

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=${{ github.repository_owner }}&show_icons=true&theme=dark&hide_border=true
          path: profile/stats.svg
          token: ${{ secrets.myrepo }}

      - name: Generate top languages card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
          options: username=${{ github.repository_owner }}&layout=compact&theme=dark&hide_border=true&hide=Jupyter%20Notebook
          path: profile/top-langs.svg
          token: ${{ secrets.myrepo }}

      - name: Generate streak and quote cards
        run: |
          mkdir -p profile
          curl -fsSL "https://streak-stats.demolab.com?user=${{ github.repository_owner }}&theme=dark&hide_border=true" -o profile/streak.svg
          curl -fsSL "https://quotes-github-readme.vercel.app/api?type=horizontal&theme=dark" -o profile/quote.svg

      - name: Commit cards
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update README cards" || exit 0
          git push
